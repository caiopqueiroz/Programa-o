// Criando um valor constante
# include <stdio.h>

int main(){
    // Usando a palavra const antes do nome da variável para torná-la imutável
    int const numero_constante = 10;
    printf("Esse e um numero guardado em uma variavel que nunca podera ser alterado: %d", numero_constante);
    return 0;
}
